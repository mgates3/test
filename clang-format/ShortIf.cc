void function() {
    if (cond)
        code();
        
    if (cond) {
        code();
    }
    
    if (cond) {
        code();
        code();
    }
    
    
    if (cond)
        code();
    else
        code();
        
    if (cond) {
        code();
    }
    else {
        code();
    }
    
    if (cond) {
        code();
        code();
    }
    else {
        code();
        code();
    }
}
