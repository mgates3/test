void e() {}
void f() { return 0; }
void g() { x = 1; return x; }
void h() { printf( "some much longer thing that really doesn't fit on one line well.\n" ); }

class Foo {
    void e() {}
    void f() { return 0; }
    void g() { x = 1; return x; }
    void h() { printf( "some much longer thing that really doesn't fit on one line well.\n" ); }
};
